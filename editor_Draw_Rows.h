#ifndef EDITOR_DRAW_ROWS_H
#define EDITOR_DRAW_ROWS_H

#include "append_Buffer_Struct.h"
#include "ab_Append.h"

// Function waits for one keypress and returns it later.
void editorDrawRows(struct appendBuffer *ab);

#endif